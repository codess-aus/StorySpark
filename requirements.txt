mkdocs>=1.5.3
mkdocs-material>=9.5.0

# Azure AI Foundry SDK for calling deployed generative models
azure-ai-inference>=1.0.0,<2.0.0
azure-core>=1.30.0

# Optional: load .env locally for credentials (not required in Actions)
python-dotenv>=1.0.1